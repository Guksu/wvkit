/**
 * `@guksu/wvkit-react/scroll-container` subpath 엔트리.
 * 코어의 three 경계(`@guksu/wvkit-core/scroll-container`)와 대칭 —
 * three 미설치 소비자가 배럴(`.`)을 크래시 없이 쓸 수 있게 값 export 를 분리한다 (B-02).
 */
export { useScrollContainer } from './components/scroll-container';
