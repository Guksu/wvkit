---
sources:
  - path: {프로젝트 루트 기준 상대 경로}
    hash: {12자리 내용 해시 — checkFreshness.mjs hash 로 계산}
updated: {YYYY-MM-DD}
---

<!--
공통 다이제스트 템플릿 — digest 스킬이 대형 파일·모듈 분석 결과를 세션 간 재사용하도록 이 형식으로 기록한다.
- {중괄호} 자리를 채우고 이 안내 주석은 삭제한다.
- frontmatter는 파일 첫 줄에서 시작해야 한다 (checkFreshness.mjs가 파싱하는 위치).
  sources 해시는 `checkFreshness.mjs hash <파일...>`로 계산한다 — 손으로 적지 않는다.
- 섹션 구조(1~4)는 추가·삭제·개명하지 않는다.
- 다이제스트는 지도다 — 수정할 파일은 원문을 읽는다. 코드 전문을 옮겨 적지 않는다.
- 시크릿(토큰·키·비밀번호)은 기록하지 않는다.
-->

# 다이제스트: {대상 모듈·파일}

## 1. 책임

{이 모듈이 무엇을 담당하는가 — 2~3문장. 처음 읽는 소비자가 아래를 이해할 최소 맥락.}

## 2. 공개 인터페이스

{다른 코드가 소비하는 것 — 함수 시그니처, 타입 shape, 엔드포인트. 소비자가 원문 없이 올바르게 호출할 수 있는 수준으로.}

## 3. 의존과 데이터 흐름

{무엇에 의존하고 누가 이것을 소비하는가 — 방향이 드러나게. 경계면(생산자↔소비자)이 QA 교차검증의 입력이 된다.}

## 4. 불변식과 함정

{지켜야 하는 가정, 어기기 쉬운 규칙, 과거에 문제가 됐던 지점. 없으면 "없음"으로 명시한다.}
