<!--
공통 루프 명세 템플릿 — loop 스킬이 반복·수렴형 작업의 루프 설계를 이 형식으로 기록한다.
- {중괄호} 자리를 채우고 이 안내 주석은 삭제한다.
- 섹션 구조(1~5)는 추가·삭제·개명하지 않는다.
- "2. 루프 설계"의 4요소는 사용자 확인 없이 실행하지 않는다.
- 시크릿(토큰·키·비밀번호)은 기록하지 않는다.
-->

# 루프: {이름}

| 항목 | 내용 |
|------|------|
| 날짜 | {YYYY-MM-DD} |
| 작성 | {에이전트 이름 또는 사용자} |
| 상태 | {설계 / 실행 중 / 성공 종료 / 안전장치 중단} |
| 실행 수단 | {/goal · /loop · Workflow 반복 · 검증자 게이트(Stop 훅)} |

## 1. 목표

{검증 가능한 종료 상태 — "누가 실행해도 기계적으로 참/거짓을 판정할 수 있는" 문장으로. 예: "npm test 전체 통과 + npx tsc 에러 0건". 기계 검증이 불가능하면 루프를 만들지 않는다.}

## 2. 루프 설계 — 사용자 확인: {YYYY-MM-DD 확인됨 / 미확인}

| 요소 | 값 |
|------|-----|
| 트리거 | {무엇이 루프를 시작하나 — 사용자 요청 1회 / 스케줄 / 이벤트} |
| 실행 단위 | {한 바퀴에 무엇을 하나 — 작게, 검증 가능하게} |
| 검증자 | {누가/어떻게 판정하나 — 실행 명령과 기준. 에이전트 자기평가 금지} |
| 종료 규칙 | {성공: 검증 전체 통과 / 실패: 아래 안전장치 도달 시 중단·보고} |

## 3. 안전장치

| 장치 | 값 |
|------|-----|
| 최대 반복 | {N}회 |
| 토큰 예산 | {N} 토큰 — 초과 시 루프를 계속하지 않고 자동 중단, 진행 상황·남은 실패·사유를 보고 후 종료 |
| 막힘 판정 | {같은 에러 N연속 등 — 막힘이면 반복을 소진하지 말고 중단·보고. 검증자 게이트 사용 시 이 N을 verifierGate.config.json의 stuckAfter로 옮긴다 — 명세에만 적으면 기계적으로 강제되지 않는다 (loop 스킬 Phase 5)} |

## 4. 실행 기록

| 반복 | 결과 | 비고 |
|------|------|------|
| 1 | {통과/실패 요약} | {막힘 징후, 예산 소모 등} |

## 5. 종료 보고

{성공/중단 여부, 중단이면 사유(토큰 예산·최대 반복·막힘), 남은 실패 목록, 후속 제안. 최종 기록은 워크로그로 남긴다.}
